<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Webtech Wheelhouse" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Webtech Wheelhouse">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

<body>
    <header>
      <nav class="navbar navbar-expand-lg bg-light rounded">
        <div class="container-fluid">
          <a class="navbar-brand" href="<%= root_path %>">Wheelhouse</a>
          
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
          </button>
          
          <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav nav-pills gap-2">
              <li class="nav-item">
                <a class="nav-link <%= 'active' if current_page?(root_path) %>" href="<%= root_path %>">Home</a>
              </li>
              <li class="nav-item">
                <a class="nav-link <%= 'active' if current_page?(standard_services_path) %>" href="<%= standard_services_path %>">Services</a>
              </li>
              <li class="nav-item">
                <a class="nav-link <%= 'active' if current_page?(visiting_path) %>" href="<%= visiting_path %>">Visit Us</a>
              </li>
              <li class="nav-item">
                <a class="nav-link <%= 'active' if current_page?(about_path) %>" href="<%= about_path %>">About Us</a>
              </li>
            </ul>
          </div>
        </div>
      </nav>
    </header>

    <main>
      <%= yield %>
    </main>
  </body>
</html>
